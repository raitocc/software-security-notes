# Lecture07 漏洞利用防护与漏洞挖掘 - 习题

> GitHub: https://github.com/raitocc/software-security-notes

> 配套答案：[Lecture07-漏洞利用防护与漏洞挖掘-习题答案.md](Lecture07-漏洞利用防护与漏洞挖掘-习题答案.md)

## A. 基础查表题

这一部分主要考防护机制、绕过技术和漏洞挖掘方法的基本概念。

### 选择题

1. ASLR 主要随机化的是：

A. 函数参数从右向左压栈的调用约定  
B. 程序源代码中的变量命名和语句顺序  
C. PE 文件中资源名称和图标显示方式  
D. 栈、堆、模块和代码基址等地址布局

2. DEP/NX 更直接阻止的是：

A. 攻击者通过信息泄漏获得模块基址  
B. 数据区中的 shellcode 被当作代码执行  
C. 程序在栈帧中保存函数返回地址  
D. Loader 在运行时解析 DLL 导入表

3. GS/Stack Canary 主要试图检测：

A. 堆块释放后是否进入正确空闲链表  
B. API 导出表中函数名称是否被重命名  
C. 栈溢出覆盖返回地址前后的连续破坏  
D. 符号执行路径约束是否存在可行解

4. ROP 能绕过单纯 DEP 的关键原因是：

A. 它先调用系统 API 修改页权限，再把数据区整体转为可执行  
B. 它通过删除返回地址，让函数调用不再依赖控制流  
C. 它让 Loader 重新加载未启用 ASLR 的系统模块  
D. 它复用已有可执行代码片段，而不是直接执行数据区新代码

5. 污点分析中的 sink 更接近：

A. 程序中最先读取外部输入的位置  
B. 不可信数据到达后可能产生危险效果的操作点  
C. 用于消除污点或约束输入的清洗函数  
D. 记录路径约束并交给求解器的表达式

6. 模糊测试更接近下列哪种方法？

A. 用符号变量代替关键输入，推导路径约束并交给求解器  
B. 根据数据依赖和控制依赖裁剪与目标无关的代码  
C. 向程序输入大量变异或生成数据，观察崩溃、超时和覆盖反馈  
D. 用正则或特征模式快速匹配危险函数调用位置

### 判断题

7. ASLR 不修复漏洞本身，它提高的是攻击者预测地址和定位 gadget 的难度。

8. Canary 能提高栈溢出利用难度，但不能替代边界检查和漏洞修复。

9. Heap Spray 是一种降低 shellcode 地址定位精度要求的利用技巧。

10. 静态分析不运行程序，通常覆盖面较广，但可能带来误报。

### 填空题

11. ROP 的全称是 `__________`。

12. SafeSEH 主要保护 Windows 的 `__________` 异常处理机制。

13. 污点分析的三类核心角色通常是 source、propagation 和 `__________`。

14. PDG 的全称是 `__________`。

## B. 理解应用题

这一部分要求你解释防护机制和漏洞挖掘方法如何工作。

### 选择题

15. 为什么信息泄漏常用于绕过 ASLR？

A. 泄露一个模块或栈地址后，可根据固定偏移推算其他运行时地址  
B. 泄露程序输出字符串后，DEP 会允许数据区代码执行  
C. 泄露源代码注释后，canary 会在函数返回前停止检查  
D. 泄露文件名后，SafeSEH 会接受任意异常处理函数

16. 对 `jmp esp` 跳板技术的理解，哪项更合理？

A. `jmp esp` 会把 shellcode 写入堆区并重新排列空闲链表  
B. `jmp esp` 主要用于解析 DLL 导出表并定位 API 函数名  
C. 返回地址被改成 `jmp esp` 后，ASLR 会固定当前模块基址  
D. 返回地址先跳到 `jmp esp`，再跳到 ESP 指向的 shellcode 位置

17. 下列哪项更接近 Heap Spray 中滑板指令的作用？

A. 在符号执行中保存每条路径的布尔约束表达式  
B. 让执行流落入一大片 NOP/类 NOP 区域后滑向 shellcode  
C. 在污点分析中标记输入字段是否经过 sanitizer  
D. 在数据流分析中区分过程内和过程间变量传播

18. API 函数自搜索的一般逻辑更接近：

A. 通过 canary 值定位返回地址，再根据返回地址计算函数参数  
B. 通过 fuzzing 覆盖率定位导入表，再随机选择 API 入口调用  
C. 通过 TEB/PEB 定位模块，再解析 PE 导出表查找目标函数地址  
D. 通过 SafeSEH 表定位 shellcode，再用 SEHOP 修复异常链

19. 关于符号执行和 fuzzing 的互补关系，下列说法更合理：

A. 符号执行主要用于随机变异输入，fuzzing 主要用于约束求解  
B. 二者都依赖手动指定每个 API 的真实运行地址  
C. 二者都主要通过检查 PE 资源节判断程序是否存在漏洞  
D. Fuzzing 善于快速探索普通路径，符号执行可求解复杂分支输入

### 简答题

20. DEP 为什么挡不住 ret2libc 或 ROP 这类代码复用攻击？

21. SafeSEH 和 SEHOP 分别更关注 SEH 攻击的哪一方面？

22. 污点分析中的 source、sink、sanitizer 分别是什么？

23. 前向切片和后向切片分别适合回答什么问题？

## C. 综合融会题

这一部分更接近考试中的推理题，需要把防护、绕过和分析方法串起来。

### 综合题 1：ROP 手推

某 ROP 链片段如下：

```text
ESP -> addr1 => POP EAX; RETN
       0x00000001
       addr2 => ADD EAX, 2; RETN
       addr3 => XCHG EAX, EDX; RETN
```

24. 执行 `POP EAX; RETN` 后，`EAX` 是多少？
25. 执行 `ADD EAX, 2; RETN` 后，`EAX` 是多少？
26. 执行 `XCHG EAX, EDX; RETN` 后，`EDX` 是多少？
27. 这个例子说明 ROP 链如何构造“新逻辑”？

### 综合题 2：防护组合

某程序存在栈溢出。攻击者能覆盖返回地址，但目标系统开启了 ASLR、DEP 和 Canary。

28. Canary 主要试图在哪一步阻断利用？
29. ASLR 会让攻击者在哪类信息上遇到困难？
30. DEP 会阻止哪种传统利用路线？
31. 攻击者为什么可能需要“信息泄漏 + ROP”的组合？

### 综合题 3：符号执行

考虑如下代码：

```c
void testme(int i) {
    int a[10];
    if (i > 0) {
        if (i > 10)
            i = i % 10;
        a[i] = 1;
    }
}
```

32. 到达 `a[i] = 1` 大致有哪些路径约束？
33. 为什么 `i = 10` 可能触发数组越界？
34. 符号执行在这里比普通随机测试多提供了什么信息？

### 综合题 4：方法选择

请为下列目标选择更合适的方法，并简要说明理由：

35. 想知道用户输入是否流入 SQL 执行函数。
36. 想用大量异常文件样本触发解析器崩溃。
37. 想求出进入某个深层分支的具体输入。
38. 想缩小某个变量相关的代码范围。

## D. 补充题

这一部分补充漏洞挖掘实践、数据流分析和 AFL 相关内容。

### 选择题

39. 在基于 IDA 的可执行文件静态检测中，分析人员发现某函数调用了 `strcpy`。下一步更合理的分析动作是：

A. 记录 `strcpy` 的导入表地址，优先判断该模块是否启用了 ASLR
B. 回溯 `strcpy` 的源和目的参数，结合局部变量空间判断是否可能越界
C. 根据 `strcpy` 的函数名确认漏洞存在，再转入 exploit 编写阶段
D. 检查该调用是否位于异常处理函数内，优先验证 SafeSEH 表项

40. 某数据流分析器用如下状态规则分析指针变量：`v.start` 遇到 `free(v)` 后变为 `v.free`，`v.free` 状态下再出现 `*v` 会标记风险。该规则更适合发现：

A. 整数运算后越过边界，可能影响长度检查结果
B. 字符串作为格式参数，可能控制格式化解析
C. 指针释放后仍被解引用，可能形成 use-after-free
D. 外部输入参与拼接，可能超过目标缓冲区空间

41. 关于 AFL 这类覆盖引导模糊测试工具，下列说法更合理的是：

A. 若某个输入没有触发崩溃，工具会优先将其作为最终漏洞证据
B. 覆盖信息主要用于恢复源代码变量名，便于人工阅读反汇编结果
C. 初始样本越偏离输入格式，越有利于稳定探索深层解析逻辑
D. 若某个变异输入带来新的执行路径，工具会倾向于保留它继续变异

### 填空题

42. 在 `afl-fuzz -i in -o out -- ./test @@` 中，`@@` 通常会被替换为当前测试用例的 `__________`。

43. 数据流分析中，不仅考虑语句执行顺序，还区分不同可达分支路径的分析通常称为 `__________` 分析。

### 简答题

44. 简述智能模糊测试相比传统随机模糊测试的改进思路。
