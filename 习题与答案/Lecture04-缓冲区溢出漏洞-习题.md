# Lecture04 缓冲区溢出漏洞 - 习题

> GitHub: https://github.com/raitocc/software-security-notes

> 配套答案：[Lecture04-缓冲区溢出漏洞-习题答案.md](Lecture04-缓冲区溢出漏洞-习题答案.md)

## A. 基础查表题

这一部分主要考课件和手册中的基本概念、流程和分类。

### 选择题

1. 在课程语境中，漏洞更贴近下列哪种含义？A

A. 可被利用并破坏安全属性的缺陷  
B. 程序运行时产生的普通输出信息  
C. 编译器生成的临时目标文件  
D. 操作系统分配的普通用户目录

2. 下列哪组更接近“空间错误 / 时间错误 / 初始化错误”的对应例子？A

A. 缓冲区溢出 / Use After Free / 未初始化内存使用  
B. 命令注入 / 认证绕过 / 默认密码  
C. 地址泄漏 / 弱口令 / SQL 拼接  
D. 资源耗尽 / 类型混淆 / 端口暴露

3. 缓冲区溢出的核心条件更接近：A

A. 写入数据超过缓冲区边界且缺少有效检查  
B. 程序加载了动态链接库并调用外部函数  
C. 用户界面展示了较长的错误提示  
D. 编译器为函数生成了栈帧结构

4. 在经典栈溢出里，攻击者常关注返回地址，主要因为：A

A. `ret` 会依据栈中的返回地址改变后续执行位置  
B. 返回地址常与栈帧中的局部变量相邻，适合保存边界检查结果  
C. 返回地址参与函数返回过程，常用于恢复调用者的局部变量  
D. 返回地址与导入函数调用有关，常用于定位外部 API

5. 下列哪一项更接近 DWORD SHOOT 的安全意义？A

A. 获得向指定位置写入指定数据的能力  
B. 通过日志系统记录异常退出的原因  
C. 将溢出数据整理成固定宽度的四字节块  
D. 通过调试器观察 DWORD 变量的值变化

6. SEH 攻击与普通返回地址覆盖的区别更接近：A

A. SEH 依赖异常处理流程触发控制流转移  
B. SEH 依赖堆块释放流程触发空闲链表合并  
C. SEH 主要通过导入表重定位改变 API 地址  
D. SEH 主要通过栈帧序言恢复旧帧指针

### 判断题

7. 缓冲区溢出可发生在栈区，也可发生在堆区。T

8. 能让程序崩溃的输入，通常还需要进一步分析可控性和防护机制，才能判断利用价值。T

9. 溢出后执行的 payload 通常以原程序当前权限运行。T

10. 同一段溢出演示代码在不同架构、编译器选项和保护机制下，运行结果可能不同。T

### 填空题

11. 漏洞的英文常用术语是 `__________`。

12. ASLR 的含义是 `__________`。

13. DEP/NX 的核心作用是限制某些内存区域被当作 `__________` 执行。

14. 栈保护中用于检测返回前栈帧是否被破坏的机制常称为 `__________`。

## B. 理解应用题

这一部分要求你把概念放回代码、流程和内存布局中理解。

### 选择题

15. 关于 `strcpy`、`strcat`、`sprintf`、`gets` 这类函数，下列说法更贴近课程内容的是：A

A. 使用不当时容易缺少边界检查，从而引发缓冲区溢出  
B. 它们主要负责解析异常处理链并恢复栈帧  
C. 它们用于在 PE 文件中查找导入函数地址  
D. 它们主要通过页表完成虚拟地址到物理地址转换

16. 下列哪项更接近“信息泄漏帮助绕过 ASLR”的原因？A

A. 泄露模块或内存地址后，可据此推算代码或数据位置  
B. 泄露错误信息后，CPU 会停止使用虚拟内存  
C. 泄露端口号后，DEP 会允许栈上代码执行  
D. 泄露用户名后，返回地址会自动变成固定值

17. `char local[4]; for (...) local[i] = argument[i];` 这类代码的风险点更接近：A

A. 循环写入缺少长度边界，输入过长会写出 `local`  
B. 局部变量位于栈上，因此不需要检查输入长度  
C. 字符数组会自动扩容以容纳更长的输入  
D. `for` 循环会自动停止在缓冲区末尾位置

18. 对于 `buff[1]; buff[2] = (int)why_here;` 课堂例子，下列理解更合理的是：A

A. 越界写可能覆盖返回地址，使函数返回时跳到 `why_here`  
B. `buff[2]` 会自动成为数组的合法元素并保存函数名  
C. `why_here` 会在编译阶段被加入函数调用顺序  
D. 返回地址变化主要由动态链接器在加载时完成

19. 为什么 64 位环境下 `(int)why_here` 可能影响示例效果？A

A. 指针宽度和 `int` 宽度不同，地址可能被截断  
B. 64 位环境中函数调用不再需要返回地址  
C. 64 位环境中数组越界访问会自动变成安全访问  
D. 指针宽度变化会使 `printf` 不再读取任何参数

### 简答题

20. 为什么说“缓冲区溢出不是字符串太长这么简单”？

21. 简述栈溢出、堆溢出在攻击目标上的常见差异。

22. 为什么任意读能力常和现代漏洞利用联系在一起？

23. 为什么 SEH 覆盖需要触发异常才能体现效果？

## C. 综合融会题

这一部分更接近考试中的推理题，需要把输入、布局、目标和防护串起来。

### 综合题 1：栈溢出推理

某函数中有如下代码：

```c
void f(char *argument)
{
    char local[4];
    for (int i = 0; argument[i]; i++)
        local[i] = argument[i];
}
```

24. 该代码的边界检查问题在哪里？
25. 如果输入超过 4 字节，可能先影响哪类内存区域？
26. 若后续覆盖到返回地址，函数返回时可能发生什么？

### 综合题 2：课堂示例分析

考虑如下代码：

```c
void why_here(void)
{
    printf("why u r here?!\n");
    exit(0);
}

void f()
{
    int buff[1];
    buff[2] = (int)why_here;
}
```

27. `buff[2]` 的问题是什么？
28. 在课件示意布局中，它可能覆盖什么控制数据？
29. 为什么这段代码在某些 64 位或开启保护的环境中未必成功跳转？

### 综合题 3：攻击流程

某攻击者先收集目标服务版本，发现可触发溢出的文件解析漏洞；随后构造样本使程序崩溃，又通过泄露地址绕过 ASLR，最后让程序执行 payload。

30. 这个过程分别对应攻击技术流程中的哪些阶段？
31. 为什么“崩溃”之后还要继续分析可控性？
32. 如果 DEP/NX 开启，攻击者为什么可能转向 ROP 思路？
